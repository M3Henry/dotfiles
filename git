[user]
	name = Henry Wilson
	email = m3henry@googlemail.com
[alias]
	dos = log --decorate --oneline --summary -s
	dogs = log --decorate --oneline --graph --summary -s --all
	a = add .
	c = commit
	s = status -sb
	p = pull --ff-only
	m = !make
[rebase]
	autosquash = true
	abbreviateCommands = true
[color "status"]
	changed = yellow
	branch = green bold
[push]
	default = simple
[pull]
	ff = only
[core]
	editor = nano --tempfile
[credential]
	helper = cache --timeout 7200
[credential "https://github.com"]
	username = m3henry
